#include <iostream>

using namespace std;

int main()
{
	
	setlocale(LC_ALL, "ru");
	int n;
	bool d = 0;
	cout << "Введите n = ";
	cin >> n;
	for (int i = 2; i * i <= n; i++)
		if (n % i == 0)
			d = 1;
	if (d == 1)
		cout << "composite";
	else
		cout << "Prime";
		return 0;
}
